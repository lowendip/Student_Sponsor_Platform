<nav class="navbar navbar-expand-lg navbar-dark bg-dark" data-bs-theme="dark">
  <div class="container-fluid">
    <div class="collapse navbar-collapse">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <% if current_user && current_user.role == "Sponsor" %>
            <a class="nav-link" href="<%= sponsor_dashboard_url %>">Dashboard</a>
          <% end %>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="<%= sponsor_projects_url %>">Sponsor Projects</a>
        </li>
      </ul>
      <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <% if current_user %>
            <%=link_to "Sign Out", sessions_delete_path, class:"nav-link", method: :delete, data: { turbo_method: :delete }%>
          <% else %>
            <%=link_to "Sign In", sessions_path, class:"nav-link"%>
          <% end %>
        </li>
      </ul>
    </div>
  </div>
</nav>
