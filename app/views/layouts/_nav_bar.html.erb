<nav class="navbar navbar-expand-lg navbar-dark bg-dark" data-bs-theme="dark">
  <div class="container-fluid">
    <div class="collapse navbar-collapse">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link" href="<%= root_url %>">Home</a>
        </li>
        <!-- Displays a dashboard link (or in the case of admin multiple) based on the user's role -->
        <% if current_user && current_user.role == "Sponsor" %>
          <li class="nav-item">
            <a class="nav-link" href="<%= sponsor_dashboard_url %>">Dashboard</a>
          </li>
        <% elsif current_user && current_user.role == "Student" %>
          <li class="nav-item">
            <a class="nav-link" href="<%= student_dashboard_url %>">Dashboard</a>
          </li>
        <% elsif current_user && current_user.role == "Admin" %>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" role="button" data-bs-toggle="dropdown">Dashboard</a>  
            <ul class="dropdown-menu">
              <li><a class="dropdown-item" href="<%= admin_dashboard_url %>">Projects</a></li>
              <li><a class="dropdown-item" href="<%= admin_users_url %>">Users</a></li>
              <li><a class="dropdown-item" href="<%= admin_domains_url %>">Domains</a></li>
            </ul>
            </li>
          <% end %>
        <!-- Displays the public projects and sponsors/students pages if the user is logged in -->
        <% if current_user %>
          <li class="nav-item">
            <a class="nav-link" href="<%= sponsor_projects_url %>">Sponsor Projects</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="<%= student_projects_url %>">Student Projects</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="<%= sponsors_url %>">Sponsors</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="<%= students_url %>">Students</a>
          </li>
        <% end %>
      </ul>
      <!-- Displays the sign in link if the user is not signed in and otherwise will display a sign out path that will remove their user session id (signing them out) -->
      <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <% if current_user %>
            <%=link_to "Sign Out", sessions_delete_path, class:"nav-link", method: :delete, data: { turbo_method: :delete }%>
          <% else %>
            <%=link_to "Sign In", sessions_path, class:"nav-link"%>
          <% end %>
        </li>
      </ul>
    </div>
  </div>
</nav>
