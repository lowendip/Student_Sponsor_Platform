<h4>Password Reset</h4>

<%= form_with url: password_reset_create_path do |form| %>
  <div>
    <%= form.label :email %>
    <%= form.email_field :email %>
  </div>

  <%= form.submit "Reset password", {class:"submit_button"} %>
<% end %>
